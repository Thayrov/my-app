import {StyleSheet} from 'react-native';
import {colors} from '../../constants/theme/colors';

export const styles = StyleSheet.create({
	titleContainer: {
		marginTop: 45,
	},
	titleContent: {
		textAlign: 'center',
		fontSize: 32,
		fontWeight: 'bold',
		color: colors.inputBgColor,
	},
});
